<%= form_for @title, :html => { :class => "form-horizontal title" } do |f| %>

  <% if @title.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@title.errors.count, "error") %> prohibited this title from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @title.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :story, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :story, :class => 'form-control' %>
    </div>
    <%=f.error_span(:story) %>
  </div>
  <div class="form-group">
    <%= f.label :date, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :date, :class => 'form-control datepicker' %>
    </div>
    <%=f.error_span(:date) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                titles_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
